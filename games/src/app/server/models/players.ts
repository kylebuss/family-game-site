// In TypeScript, we can define interfaces to represent the structure of our data.
// This structure should match the data we expect to receive from the API/Database.
export interface Player {
  id: number;
  name: string;
}
